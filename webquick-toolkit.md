# プライバシーポリシー / Privacy Policy

## 1. ユーザーデータの取得と利用目的 (Data Collection and Usage)
本拡張機能「**WebQuick Toolkit**」（旧 Fast login for WP）は、WordPressの運用・ログイン支援およびWeb制作・検証をブラウザ上で効率化するためのツールです。
ユーザーが登録・設定・入力した以下の情報およびデータは、すべてユーザーのブラウザ内（`chrome.storage.local`）にのみローカルに保存され、開発者を含む第三者の外部サーバーへ収集・送信・蓄積されることは一切ありません。

*   **サイト管理データ**: 登録されたサイト名、URL、環境識別ラベル、ピン留め状態、備考メモ
*   **各種設定情報**: リンク遷移の動作設定（別タブ/同タブ）、Admin Bar（管理バー）の表示・非表示設定
*   **メモデータ**: Markdownメモエディタに入力・保存されたテキスト内容
*   **各種変換ツール入力**: HTML整形、CSS/SCSS雛形生成、テキスト変換等の処理はすべてブラウザ内部（クライアントサイド）でのみ実行され、入力されたテキストやコードが外部に送信されることはありません。

## 2. 権限（パーミッション）の使用目的 (Permissions)
本拡張機能は、機能提供のために以下の最小限の権限のみを使用します。

*   **アクティブタブの取得権限 (`activeTab`)**:
    現在開いているウェブページのURLとタイトルを取得し、WordPressサイトの自動検知、ログイン・管理画面URLの生成、Meta情報（Title/Description）の取得・コピー、およびサイトの簡単追加登録を実行するために使用します。ユーザーのブラウジング履歴を常時追跡・収集することはありません。
*   **ストレージへのアクセス権限 (`storage`)**:
    ユーザーが登録したサイト一覧データ、Admin Bar設定、Markdownメモデータ、各種オプション設定をブラウザ内に安全に保存し永続化するために使用します。
*   **サイドパネル表示権限 (`sidePanel`)**:
    Web制作支援ツール、サイト管理リスト、Markdownメモエディタなどの各種UIをChromeのサイドパネルとして常時表示し、Webページを閲覧しながら作業できるようにするために使用します。
*   **スクリプト実行権限 (`scripting`)**:
    閲覧中のWebページに対して、WordPressのAdmin Barの表示/非表示を切り替えるCSS/スクリプトの動的適用や、Metaタグ情報・WordPressプラグインの検知スクリプトを実行するために使用します。
*   **ホスト権限 (`http://*/*`, `https://*/*`)**:
    ユーザーが制作・管理・検証を行う任意のHTTP/HTTPSサイト上で、ページ読み込み時のAdmin Bar非表示スタイルの自動適用や検証スクリプトを正しく動作させるために使用します。

## 3. データの保護と第三者への開示 (Data Protection and Disclosure)
本拡張機能で処理されるすべてのデータ（サイト情報、メモ内容、変換対象テキスト、Meta情報等）は、拡張機能の目的であるWebサイト管理・制作・検証支援に必要な範囲でのみクライアントサイドで処理されます。これらのデータを開発者や第三者が閲覧、追跡、収集、販売、共有することは一切ありません。

## 4. お問い合わせ (Contact)
本拡張機能に関するお問い合わせは、本リポジトリのIssueまたは開発者までご連絡ください。