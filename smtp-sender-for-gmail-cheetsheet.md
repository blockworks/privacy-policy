# キーボードショートカット & 短縮入力チートシート

## 1. 短縮入力（本文入力時）

※全角入力（日本語入力モード）のままでも動作します。

| キー | 全角対応 | 動作 |
| :--- | :--- | :--- |
| `;;d` | `；；ｄ` | **日付選択フォーム** を表示して挿入 |
| `;;w` | `；；ｗ` | **和暦日付選択フォーム** を表示して挿入 |
| `;;t` | `；；ｔ` | **時刻選択フォーム** を表示して挿入 |
| `;;1` 〜 `;;0` | `；；１` 〜 `；；０` | 設定した **スニペットテキスト** を直接展開 |
| `;;i` | `；；い` | 登録済み **スニペット一覧** を表示して選択挿入 |
| `;;s` | `；；せ` | **送信元情報（メールアドレス / 表示名 / 署名）** を選択挿入 |

---

## 2. エディタ操作ショートカット（本文入力時）

| ショートカット | 動作 |
| :--- | :--- |
| `Shift + Enter` | **箇条書き・連番の自動継続**<br>・数字連番: `1. `（`2. `, `3. `...）<br>・丸数字連番: `①`〜`⑳`（`②`, `③`...）<br>・箇条書き記号: `・`, `○`, `●`, `- `, `* ` |
| `Alt + ↑` / `Alt + ↓` | 選択行（またはカーソル行）を **上下に移動** |
| `Ctrl + Alt + ↑` / `Ctrl + Alt + ↓` | 選択行（またはカーソル行）を **上下に複製** |
| `Alt + ]` | 選択行の **引用符（`> `）を1レベル追加** |
| `Alt + [` | 選択行の **引用符（`> `）を1レベル削除** |

