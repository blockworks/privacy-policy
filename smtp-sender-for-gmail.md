# プライバシーポリシー / Privacy Policy

## 1. ユーザーデータの取得と利用目的 (Data Collection and Usage)
本拡張機能「**SMTP Sender for Gmail**」は、ユーザーが設定した外部の送信APIを経由してメールを送信するためのツールです。
ユーザーが設定画面で入力した設定情報（送信先API URL、APIトークン、送信元メールアドレス、送信者名、署名）は、すべてユーザーのブラウザ内（`chrome.storage`）にのみ安全に保存され、開発者を含む第三者のサーバーへ収集・蓄積されることは一切ありません。

## 2. 権限（パーミッション）の使用目的 (Permissions)
本拡張機能は、動作のために以下の権限を使用します。

*   **Gmailへのアクセス権限 (`https://mail.google.com/*`)**:
    GmailのWeb画面上に「送信モード切り替えトグル」および「独自のメール作成・送信ポップアップ」を表示するために使用します。メール本文や宛先情報を読み取りますが、これはメールの送信処理のみに使用され、外部に不正に送信・共有されることはありません。
*   **外部ホスト権限 (`https://*/*`)**:
    ユーザー自身が設定した任意の外部SMTP送信APIサーバーに対して、メール送信リクエスト（Fetch通信）を実行するために使用します。

## 3. データの保護と第三者への開示 (Data Protection and Disclosure)
処理されるメールデータ（宛先、件名、本文）および設定情報は、メール送信処理の実行に必要な範囲でのみ処理されます。これらを開発者や第三者が閲覧、追跡、販売、共有することは一切ありません。

## 4. お問い合わせ (Contact)
本拡張機能に関するお問い合わせは、本リポジトリのIssueまたは開発者までご連絡ください。
